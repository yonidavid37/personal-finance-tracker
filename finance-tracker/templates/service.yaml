apiVersion: v1
kind: Service
metadata:
  name: {{ include "finance-tracker.fullname" . }}
  labels:
    {{- include "finance-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finance-tracker.selectorLabels" . | nindent 4 }}
