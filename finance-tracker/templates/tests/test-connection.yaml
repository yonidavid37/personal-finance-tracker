apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "finance-tracker.fullname" . }}-test-connection"
  labels:
    {{- include "finance-tracker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "finance-tracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
